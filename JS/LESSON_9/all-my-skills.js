let firstAddendum = 13;

const secondMultiplier = 11;

let finalExpression = ((firstAddendum + 17) / (27 - 24)) * secondMultiplier;

const firstSentence = 'Урок 9. Задание 3.2.';

const secondSentence = "Результат вычислений:";

console.log(`${firstSentence} ${secondSentence} ${finalExpression}.`);